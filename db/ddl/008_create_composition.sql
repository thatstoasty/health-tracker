CREATE TABLE TRACKER.COMPOSITION (
    SUBMITTED_ON CHAR(10) DEFAULT CURRENT_DATE::CHAR(10) PRIMARY KEY NOT NULL,
    WEIGHT DECIMAL NOT NULL,
    BODYFAT DECIMAL NOT NULL,
    NECK DECIMAL,
    SHOULDERS DECIMAL,
    LEFT_BICEP DECIMAL,
    RIGHT_BICEP DECIMAL,
    LEFT_TRICEP DECIMAL,
    RIGHT_TRICEP DECIMAL,
    LEFT_FOREARM DECIMAL,
    RIGHT_FOREARM DECIMAL,
    CHEST DECIMAL,
    WAIST DECIMAL,
    LEFT_QUAD DECIMAL,
    RIGHT_QUAD DECIMAL,
    LEFT_CALF DECIMAL,
    RIGHT_CALF DECIMAL,
    CRET_TS TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UPDT_TS TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL
);